# Handoff — SplitIT

**Обновлён:** 2026-08-01
**Роль, сдающая работу:** ① Claude Code → ② CODEX
**Ветка:** `chore/gate-canary-realtime` (**не запушена** — с машины ① нет прав на push, см. §6)
**Правила конвейера:** `.agents/rules/antigravity2_core.md`, `.agents/rules/collaboration.md`

---

## 1. Что сделано

Закрыты три инфраструктурные задачи из прошлой передачи: гейт в CI, canary
после деплоя и сценарий Realtime. Продуктовый код не менялся — ни `src/`, ни
миграции.

Из трёх задач доказаны две. Canary прогнан на боевом адресе (18/18) и проверен
на обратимость. Realtime прогнан на боевом проекте (25/25) — последнее белое
пятно закрыто, §5. Гейт написан и проходит схему GitHub Actions, но ни разу не
запускался: с этой машины нет прав на push.

---

## 2. Изменённые файлы

| Файл | Что это |
|---|---|
| `scripts/lib/supabase-verify.mjs` | **Новый.** Общая обвязка живых проверок: конфигурация, HTTP к PostgREST и GoTrue, временные аккаунты, уборка. |
| `scripts/verify-production.mjs` | Переведён на общую обвязку. Сценарий из 24 проверок не изменился ни строкой. |
| `scripts/verify-realtime.mjs` | **Новый.** `npm run verify:realtime` — Postgres Changes на трёх живых сессиях и четырёх каналах. Прогнан: 25/25. |
| `scripts/canary.mjs` | **Новый.** `npm run canary` — читающая проверка задеплоенного приложения. |
| `.github/workflows/gate.yml` | **Новый.** lint, типы, unit, RLS, E2E на каждый push и PR в `main`. |
| `.github/workflows/canary.yml` | **Новый.** Canary по deployment_status от Vercel, раз в сутки и руками. |
| `package.json` | Две команды: `verify:realtime`, `canary`. |
| `progress.md`, `todo.md` | Состояние приведено к проверенному. |

`bug_report.md` в рабочем дереве изменён не мной: это параллельная работа
Чекера по мобильным артефактам (MOB-1…MOB-7). Файл не тронут и не закоммичен —
он принадлежит роли ③.

---

## 3. Что теперь обязано быть истинным

**Гейт больше не держится на дисциплине.** Ветка с красным lint, типами, unit,
RLS или E2E видна на GitHub до слияния. Двух проверок из семи в CI нет
осознанно: `npm run build` требует настоящий `.env.local`, которого в CI нет,
а `serve:out` — ручной smoke без кода возврата. Первую заменяет canary снаружи.

**После деплоя прод проверяется снаружи.** `npm run canary` ловит два сбоя,
которых не видит ни один локальный гейт: выкатку без переменных Supabase и
выкатку без применённых миграций.

**Сценарий Realtime не может тихо устареть.** Скрипт воспроизводит подписку
приложения, а не импортирует её (модуль завязан на браузер), поэтому копия
сверяется с оригиналом: список таблиц разбирается из `subscribeToGroup`, и
расхождение валит прогон на шаге 0. Проверено: переименование одной таблицы
в `remote-store.ts` роняет проверку.

**У живых проверок одна уборка на всех.** Она удаляет событие service-role
запросом, аккаунты — Auth Admin API, и проверяет остаток тем же
привилегированным контекстом. Своя копия в новом скрипте повторила бы дефект
ложноположительной anon-проверки, который в прошлом круге уже чинили.

---

## 4. Доказательства

```
npm run lint          0
npx tsc --noEmit      0
npm run test:unit     54 passed
npm run test:rls      34 passed
npm test              92 passed (mobile + desktop)
npm run canary        18/18 на https://split-it-ere9.vercel.app
```

Canary на сборке без ключей (`build:test`, поднята на :4199 — это ровно случай
«деплой без переменных Supabase»):

```
✓ все маршруты отвечают 200 — 12 шт.
✓ все чанки страниц загрузились — 25 шт.
✗ в сборке настоящий адрес проекта Supabase (сетевой режим)
✗ в сборке есть публикуемый ключ
✗ в сборке нет конфигурации Supabase
Пройдено: 3   Провалено: 3      exit=1
```

Зелёная проверка, которая не краснеет ни на чём, — не проверка. Эта краснеет.

Различение «схемы нет» и «схема закрыта» подтверждено живыми ответами боевого
PostgREST: несуществующая таблица — `404 PGRST205`, несуществующая функция —
`404 PGRST202`, существующая функция без прав у анонима — `401 42501`. Все
четыре RPC отвечают именно `42501`, то есть И-19 держится на проде прямо сейчас.

Оба workflow проходят схему GitHub Actions (`@action-validator/cli`).

---

## 5. Realtime проверен. 25/25 на боевом проекте

Последнее белое пятно закрыто. `npm run verify:realtime` на
`jrarbbfsqrkjckujfpcz`:

```
✓ четыре канала подтвердили подписку — SUBSCRIBED, 5.7s
✓ общий канал живой: изменение группы дошло до владельца и участника
✓ сокет постороннего живой: своя группа события доставляет
✓ владелец получил событие о чужом расходе без перезагрузки
✓ доставка заняла — 202 мс
✓ на чужой группе посторонний не увидел ни одного события
✓ в ту же минуту его сокет доставляет события из своей группы
✓ отписавшийся участник больше событий не получает
Пройдено: 25   Провалено: 0
```

Уборка отработала: обе группы и три аккаунта удалены, отсутствие проверено
привилегированным контекстом.

### Первый прогон дал 17/18 и вскрыл дефект в тесте, а не в приложении

Запись участника не дошла до владельца за 20 секунд. При этом позже, на той же
подписке, события пошли — и у владельца к моменту проверки не было ни одного
события вообще, включая неотфильтрованные `expense_splits`. Канал молчал
целиком, а не выборочно, поэтому объяснение «владелец не видит чужих строк»
отпадало.

Причина: `SUBSCRIBED` подтверждает, что канал присоединён, но не что серверная
подписка на изменения уже установлена. Запись в этом окне не долетает и не
переигрывается — Postgres Changes не хранит историю.

Важнее сам провал теста. Вместе с красной проверкой доставки **зеленела**
проверка «посторонний ничего не увидел». Посторонний не увидел ничего потому,
что не увидел никто. Это тот же класс дефекта, что anon-проверка уборки в
прошлом круге: проверка, проходящая по той же причине, по которой падает
соседняя, не проверяет ничего.

Что изменено в тесте:

- канал считается живым только после реально прошедшего через него события;
  до прогрева не проверяется ни одно утверждение;
- у постороннего появилась **своя группа со своим каналом**: его молчание на
  чужой группе засчитывается лишь тогда, когда его же сокет в тот же промежуток
  доставляет события из своей;
- каждое событие пишется с временем и владельцем строки, в конце печатается,
  что получил каждый канал.

### Находка: канал группы скоуплен не полностью

В сводке видно, что на канал чужой группы посторонний получил
`expense_splits:INSERT`. Разобрано и проверено: это его собственная строка из
его собственной группы. В `expense_splits` нет `group_id`, поэтому
`subscribeToGroup` подписывается на таблицу без фильтра и отбор оставляет RLS —
привязка не знает про группу, а RLS законно пропускает свои строки.

Утечки нет: добавлена проверка принадлежности, и по итогам всего прогона на
чужом канале нет ни одной строки, не принадлежащей самому постороннему.

Но канал группы при этом не полностью скоуплен: подписчик получает на канал
одной группы события из всех остальных своих групп и делает лишний перечит.
Направление — денормализовать `group_id` в `expense_splits` новой миграцией,
проиндексировать и добавить фильтр в подписку. Это моя зона, но отдельной
веткой: схема данных не должна меняться в ветке про CI и canary.

---

## 6. Разбор отчёта Чекера по этой ветке

Чекер принял локальный гейт и canary, завёл два новых S3 и оставил три пункта
неприкрытыми. Разбор по пунктам.

### CHK-1 — `/favicon.ico` отдаёт 404. Подтверждаю, зона ④

```
https://split-it-ere9.vercel.app/favicon.ico → 404
```

В `out/index.html` нет ни одного `<link rel="icon">`, каталог `public/` пуст, в
`metadata` иконки не объявлены. Браузер запрашивает `/favicon.ico` сам, поэтому
ошибка в консоли есть на каждой загрузке любой страницы.

Чинится одним файлом `src/app/icon.svg` — Next сам выпустит тег и уберёт 404.
Марка уже существует и придумывать её не нужно: в `layout.tsx` это градиент от
`blue-600` к `indigo-500`, скруглённый квадрат, белая жирная «S». Перенос
готового решения в файл — работа ④, я в разметку и палитру не хожу.

### CHK-2 — кнопка под навигацией. Воспроизвёл, зона ④

Замер на собранном `out/`, Chromium 375×812, демо-сессия без событий:

```
вверху страницы:  кнопка y=717.5…757.5   панель y=734…800   перекрытие 23.5 px
после прокрутки:  кнопка y=506.5…546.5   панель y=734…800   перекрытия нет
страница прокручивается на 211 px, после прокрутки кнопка принимает клик
```

Числа сходятся с отчётом с точностью до способа замера, дефект реальный.

Но механизм — тот же, что у S3-3, и вывод прошлого круга остаётся в силе:
инвариант «до содержимого можно доскроллить» держится. Отступа снизу хватает.
`main` получает `padding-bottom: 120px`, навигация занимает нижние 78 px, то
есть последний элемент после полной прокрутки уходит из-под панели с запасом
42 px. Кнопка перекрыта не из-за нехватки отступа, а потому что на нулевой
прокрутке она находится в середине документа, а панель зафиксирована.

Отличие от S3-3 всё же есть, и оно делает пункт осмысленным: на пустом экране
эта кнопка — единственное действие, и первое, что видит новый пользователь, —
наполовину закрытый призыв к действию. Это вопрос компоновки первого экрана,
а не отступа: до карточки пустого состояния идут карточка «вы вошли как»,
плитка расходов, поиск и фильтры. Что из этого сжать на узком экране — решение
④, не моё.

**Побочная находка для ④.** Открытый вопрос прошлой передачи закрыт замером:
в `main` классы `pb-32` и `pb-[calc(7.5rem+env(safe-area-inset-bottom,0px))]`
стоят одновременно, и выигрывает второй — вычисленный отступ 120 px, а не 128.
`pb-32` мёртв и его можно убрать, ничего не изменится.

### Три пункта, которые Чекер не смог закрыть

Ни один не чинится кодом: всем нужен доступ, которого нет ни у меня, ни у
Чекера. Пока их не закроют, ветку принимать нельзя — и Чекер прав, что не
принял.

| Пункт | Чего не хватает | К кому |
|---|---|---|
| ~~`verify:realtime` — первый живой прогон~~ | **закрыто, 25/25 — см. §5** | — |
| `verify:prod` после смены обвязки | `SUPABASE_SERVICE_ROLE_KEY` либо отдельный проект-песочница (`VERIFY_SUPABASE_URL` + `VERIFY_SUPABASE_ANON_KEY`) | владелец |
| Первый прогон гейта на GitHub | ключ, которому разрешён push в `alexflin777-svg/SplitIt` | владелец |

Без ключа скрипт краснеет намеренно: он не может убрать за собой временные
аккаунты, и оставить их в боевом проекте молча — хуже, чем показать провал.

### Чего я в canary не добавил

Проверку `/favicon.ico` — сознательно. Она бы краснела до тех пор, пока ④ не
положит иконку, а canary, который постоянно красный по известному S3, перестают
читать. Добавить стоит вместе с иконкой, одним заходом.

---

## 7. Что должен проверить Чекер

1. **Запушить ветку и посмотреть на гейт.** С моей машины push невозможен:
   `git@github.com: Permission denied (publickey)`, `gh` не установлен. Пока
   workflow не отработал на GitHub, «гейт настроен» — утверждение без
   доказательства. Валидная схема этого не заменяет.
2. **`SUPABASE_SERVICE_ROLE_KEY=… npm run verify:realtime`** на боевом проекте.
   Прогон заводит три временных аккаунта, одно событие и удаляет их за собой.
   Ожидается: три канала подтверждают SUBSCRIBED, запись участника долетает до
   владельца, изменение группы — до участника, посторонний не получает ничего,
   отписавшийся перестаёт получать события. Только после этого Realtime можно
   считать проверенным слоем.
3. **`SUPABASE_SERVICE_ROLE_KEY=… npm run verify:prod`** — те же 24/24, что и в
   прошлом круге. Сценарий не менялся, но обвязка под ним новая, и это стоит
   подтвердить прогоном, а не чтением диффа.
4. **`npm run canary`** после первого же деплоя ветки — проверить, что
   `deployment_status` действительно приходит от Vercel и workflow срабатывает.
   Если интеграция Vercel не создаёт Deployments в этом репозитории, останутся
   расписание и кнопка, а триггер надо будет заменить.

---

## 8. Осознанно не сделано

- **Живые прогоны `verify:realtime` и `verify:prod` я не выполнял.** Границу
  задала прошлая передача: инструмент пишет ①, аккаунты в боевом проекте
  заводит ③. Тогда это и вскрыло дефект уборки — прогон чужими руками нашёл то,
  чего автор не искал.
- **Canary не проверяет содержимое страниц.** Только коды ответов, состав
  бандла и состояние бэкенда. Проверка «на экране видно то, что нужно» — это
  Playwright по `out/`, он уже есть, и дублировать его снаружи незачем.
- **Canary не следит за свежестью деплоя.** Сравнивать `BUILD_ID` прода с
  локальным нечем: у ветки и у прода они разные по определению. Отставший
  деплой ловится тем, что workflow привязан к `deployment_status`, а не тем,
  что canary угадывает версию.
- **Гейт не гоняет `npm run build` и `serve:out`** — см. §3, причина в
  отсутствии секретов и в отсутствии кода возврата соответственно.
- **`bug_report.md` не тронут.** Там открыты MOB-1…MOB-7 по мобильным
  артефактам — зона ④ Gemini, и содержательно они к этой ветке не относятся.
  Замечание MOB-5 (handoff устарел) этой передачей закрывается: `main` сейчас
  `4ae8acd`, эта ветка ушла от него на четыре коммита.
- **`pb-32` и `pb-[calc(...)]` одновременно** в `layout.tsx` по-прежнему
  оставлены как есть — зона ④, не блокирует.

---

## 9. Открытые задачи

| Задача | Зона |
|---|---|
| Push ветки и первый прогон гейта на GitHub | владелец → ③ Чекер |
| Повторный `verify:prod` после смены обвязки | ③ CODEX/Чекер |
| `group_id` в `expense_splits`: скоупить канал группы полностью | ① Claude Code, отдельной веткой |
| CHK-1: иконка приложения, `/favicon.ico` отдаёт 404 | ④ Gemini |
| CHK-2: компоновка первого экрана на 375×812 + мёртвый `pb-32` | ④ Gemini |
| MOB-1…MOB-3: IPA без подписи, APK/IPA вне репозитория | ④ Gemini |
| Защита от скомпрометированных паролей | владелец |
| Supabase → URL Configuration (Site URL, Redirect URLs) | владелец |
| Решение про подтверждение email | владелец |

---

## Приложение: прошлые передачи

### 2026-08-01 — ① Claude Code: закрытие последнего блокера

`npm run verify:prod` на боевом Supabase — 24/24. GoTrue, PostgREST, атомарные
RPC и RLS подтверждены двумя настоящими сессиями; независимый SQL-аудит после
прогона показал нулевой остаток тестовых данных. S3-2 и S3-3 влиты. Расчёты
покрыты 54 unit-тестами на свойства, найден и исправлен дефект округления курса.

Главный урок круга: проверка удаления в контексте, который не видит удаляемое,
всегда зелёная. Anon-запрос не показывал оставшуюся группу из-за RLS, и уборка
рапортовала об успехе на живых строках. Проверять результат надо в контексте,
который **увидел бы провал**.

Второй урок: регрессионный тест на S3-3 проходил и до правки, и после —
перекрытие воспроизвести не удалось. Тест оставлен, но переименован из
регрессии в инвариант: зелёный тест с надписью «регрессия» создаёт ложное
покрытие, а это хуже, чем его отсутствие.

### 2026-08-01 — ③ CODEX/Чекер: live production verification

`npm run verify:prod` выполнен на Supabase `jrarbbfsqrkjckujfpcz`: 24/24. После
повторного прогона независимый SQL-аудит показывает нулевой остаток тестовых
users/groups/members/expenses; штатный Auth-пользователь — 1.

Исправлен дефект verifier'а: anon-проверка скрывала оставшуюся группу через RLS
и ложно подтверждала каскад. Теперь cleanup запускается из `finally`, удаляет
группу service-role запросом и проверяет отсутствие тем же контекстом.

### 2026-08-01 — ③ CODEX/Чекер: две реалистичные группы

Испытания группы из 4 автопутешественников РФ → Турция и группы из 10
организаторов выпускного. Расчёты 260 000 ₽ / 2 перевода и 500 000 ₽ /
9 переводов подтверждены; данные двух групп не смешиваются. Gate: lint 0,
TypeScript 0, unit 54/54, RLS 34/34, build 15 страниц, Playwright 70/70.
Подробности: `realistic_scenarios_report.md`.
