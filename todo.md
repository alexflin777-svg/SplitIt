# SplitIT — актуальный backlog после проверки

> **Распределение ролей в команде:**
> - 🧠 **Cloud (Claude)**: Разработка кода и бэкенда.
> - 🔍 **Codex**: Код-чекер, QA и верификация RLS.
> - 🎨 **Gemini (Вы)**: UX/UI Дизайн, верстка, нативная сборка (iOS/Android), деплой и Git-коммиты.

Источник истины по дефектам и доказательствам: `bug_report.md`.

## Блокирует релиз

- [ ] Закрыть RLS: убрать `FOR ALL USING (true)`, добавить `WITH CHECK`, защитить
  `group_invites`, проверить двумя аккаунтами на чистом Supabase.
- [ ] Реализовать серверный invite token и загрузку одной реальной группы в
  чистом браузерном контексте.
- [ ] Удалить симуляцию OTA или подключить реальный updater с проверяемым
  version/build hash.

## Обязательные регрессии

- [ ] Отрицательные/нулевые/нечисловые расходы и погашения не сохраняются.
- [ ] `/auth?mode=login` и `/auth?mode=reset` открывают правильную вкладку.
- [ ] Playwright использует один заранее собранный `out/` и падает при занятом
  чужим процессом порте.
- [ ] `npm run lint` работает без интерактивного мастера.
- [ ] Ошибка OCR показывает ручной режим и не зависает на «Сканирование…».

## Проверено в текущем артефакте

- [x] `npx tsc --noEmit` проходит.
- [x] `npm run build` генерирует 14 статических страниц.
- [x] 28 существующих Playwright-тестов проходят на mobile и desktop.
- [x] Созданный локально runtime-id события открывается через query-route.
- [x] Неверный локальный пароль не создаёт сессию.

## Требует внешнего окружения владельца

- [ ] Подключить настоящий Supabase и применить только исправленную миграцию.
- [ ] Проверить синхронизацию двумя реальными аккаунтами/устройствами.
- [ ] Проверить восстановление пароля по реально полученному письму.
- [ ] Задать AlloyDB/Google Cloud параметры и завершить OAuth MCP-серверов.

Исторические отметки про «5 реальных пользователей», рабочую Supabase-
синхронизацию, 18/18 страниц и готовый APK не считаются подтверждёнными текущим
кругом и не перенесены как выполненные.
